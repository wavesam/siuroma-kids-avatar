// Canvas dimensions (design size)
export const CANVAS_WIDTH = 800;
export const CANVAS_HEIGHT = 800;
export const CANVAS_ASPECT = CANVAS_WIDTH / CANVAS_HEIGHT;

// Avatar display size in tabs
export const AVATAR_DISPLAY_SIZE = 300;

// Layout constants
export const LAYOUT_MARGIN = 48;
export const HEADER_RESERVE = 220;

// Special item IDs
export const DRAWING_LAYER_ID = "drawing-layer";

// Drawing history
export const MAX_DRAWING_HISTORY = 50;
